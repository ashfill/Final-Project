@model IEnumerable<CarProject.Manufacturer>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ManufactuerName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ManufactuerName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ManufactuerID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ManufactuerID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ManufactuerID })
        </td>
    </tr>
}

</table>
