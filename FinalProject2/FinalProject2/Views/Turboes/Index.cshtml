@model IEnumerable<FinalProject2.Turbo>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HPRating)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TurbineSize)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HPRating)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TurbineSize)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TurboID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TurboID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TurboID })
        </td>
    </tr>
}

</table>
